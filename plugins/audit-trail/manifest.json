{
  "id": "audit-trail",
  "name": "Audit trail",
  "description": "Comprehensive audit logging for all VerifyWise activities. Track who did what, when, and why. Generate detailed audit reports for compliance and security reviews.",
  "version": "1.0.0",
  "author": {
    "name": "VerifyWise",
    "url": "https://verifywise.ai"
  },
  "type": "feature",
  "tags": ["audit", "logging", "compliance", "security", "tracking", "history"],
  "permissions": ["events:listen", "database:read", "database:write", "config:read"],
  "config": {
    "retentionDays": {
      "type": "number",
      "default": 365,
      "min": 30,
      "max": 2555,
      "label": "Retention period (days)",
      "description": "How long to keep audit logs"
    },
    "logUserActions": {
      "type": "boolean",
      "default": true,
      "label": "Log user actions",
      "description": "Track user logins, logouts, and session activity"
    },
    "logDataChanges": {
      "type": "boolean",
      "default": true,
      "label": "Log data changes",
      "description": "Track all create, update, and delete operations"
    },
    "logAccessAttempts": {
      "type": "boolean",
      "default": true,
      "label": "Log access attempts",
      "description": "Track both successful and failed access attempts"
    },
    "alertOnSuspicious": {
      "type": "boolean",
      "default": false,
      "label": "Alert on suspicious activity",
      "description": "Send alerts when suspicious patterns are detected"
    }
  },
  "ui": {
    "dashboardWidgets": [
      {
        "id": "audit-activity-widget",
        "template": "activity-feed",
        "title": "Recent audit events",
        "endpoint": "/recent",
        "config": {
          "maxItems": 10
        }
      }
    ]
  },
  "compatibility": {
    "minVersion": "1.6.0"
  }
}
